# 前端工程治理与演进路线

更新时间：2026-08-23

## 目的

本文档记录 `workflow-agent-web` 当前的工程成熟度、潜在风险、长期约束和下一步整改顺序。后续页面、工作流能力和 Agent 功能必须在不破坏本文档约束的前提下演进。

项目当前已经具备统一请求层、租户化查询缓存、路由权限、功能目录、设计系统、单元测试和 Playwright 测试，能够支持内部试用。生产就绪仍取决于身份安全、真实前后端集成、契约治理、可访问性和大文件拆分。

## 本轮已完成

- 统一分页入口：列表页和流程实例详情统一使用 `TablePagination`，避免分页高度、标签和可访问性属性在页面之间漂移。
- 统一表格基线：普通文本左对齐，状态/版本标签按单元格语义居中，统一表头字重、内容垂直对齐和空列表最小高度。
- 统一交互基线：增加全局键盘焦点样式和 `prefers-reduced-motion` 支持。
- 收敛状态和空状态颜色：公共组件优先使用设计 token，补充 Agent 恢复状态语义。
- 拆出 Agent Schema 字段编辑组件与 Schema composable，页面不再独立维护两套重复的字段表格和 JSON 构造逻辑。
- 修正认证页会话文案，不再向用户暗示 Token 会保存到浏览器可读存储。
- 桌面端侧边栏支持收起/展开，并记忆用户偏好；移动端仍使用抽屉导航，避免两套导航模型互相影响。
- BPMN 编辑器和流程图查看器的样式、字体资源改为路由级加载，普通列表首屏不再加载 BPMN 资源。
- BPMN 编辑器和流程图容器增加尺寸观察、画布边界隔离和容器尺寸约束，避免侧边栏切换、窗口缩放和移动端布局变化后节点或浮层错位。
- 增加包体预算脚本并接入 `check`，JavaScript 单文件和样式单文件超限会使质量检查失败。
- 增加侧边栏收起/展开的 Playwright 回归用例，并保留核心页面的 axe 检查入口。
- 流程设计器的 Agent 配置面板、Agent 管理页的 Provider 表格已拆为独立业务组件；设计器和 Agent 页面本轮涉及的颜色、表面和状态色已迁移到设计 Token。

## 不可破坏的工程约束

### 视觉方向

产品定位为“Human-Agent Workflow 控制台”：面向业务操作人员、租户管理员和平台管理员，核心任务是看懂并推进一次流程运行。视觉上采用深靛蓝工作台、冷白数据画布和蓝色流程信号线；装饰保持克制，流程状态轨迹是唯一的识别性视觉元素。普通文字左对齐，状态和版本标签在各自单元格内居中，页面不得通过局部自定义样式改变这一约定。

### 功能边界

1. 页面和业务组件必须归属明确的 `features/<domain>`，不得把业务逻辑放回全局组件或 `App.vue`。
2. 每个 feature 应逐步建立公开入口；跨 feature 只能依赖公开类型、组件或用例，不得任意引用对方内部文件。
3. View 只负责页面编排。复杂状态、请求组合、表单规则和 BPMN 操作必须下沉到 composable、领域函数或独立组件。
4. 通用组件只能包含稳定的视觉和交互语义，不得携带特定业务接口调用。

### API、状态和租户

1. 所有 HTTP 请求必须经过统一 `apiClient`；不得在页面中直接创建 Axios/Fetch 客户端。
2. 所有租户敏感的服务端状态必须使用包含 `tenantCode` 的 Query Key。
3. 切换租户时必须清理或隔离旧租户缓存、临时表单、选人结果和 BPMN 编辑状态。
4. Pinia 只保存真正的客户端会话或跨页面 UI 状态；服务端状态由 TanStack Query 管理。
5. 接口模型必须由 OpenAPI 契约生成或校验；不得长期依赖前后端手工维护同名类型。

### 设计系统与交互

1. 颜色、间距、圆角、阴影、字号和控件高度必须来自设计 Token 或统一组件，不得在页面重复定义近似值。
2. 同一语义的按钮、状态标签、筛选栏、分页器、弹窗和表单字段必须使用同一组件和布局模式。
3. 原生可访问语义优先；图标按钮必须有名称，键盘焦点必须可见，弹窗必须正确管理焦点。
4. 选人和流程配置不得暴露 Flowable 表达式、内部变量名或后端实现细节。
5. 大数据选择器必须使用服务端搜索、分页和防抖，不能一次加载全部用户。

### 安全

1. 浏览器不得存储服务端服务客户端密钥，也不得生成 `X-Workflow-Token`。
2. 页面权限只用于用户体验；后端权限校验始终是最终安全边界。
3. 未经安全评审不得使用 `v-html`、动态脚本、未净化 SVG/HTML 或把 Token 输出到日志。
4. 浏览器认证使用 HttpOnly Cookie；`localStorage` 只允许保存非敏感会话展示信息，不得重新存入访问 Token。

## 潜在问题与整改方向

| 优先级 | 问题                                 | 影响                                                                                               | 整改方向                                                                                                                                                      | 验收标准                                                                                           |
| ------ | ------------------------------------ | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| P0     | Playwright 主要使用模拟 API          | 无法验证真实鉴权、迁移、租户切换和接口契约                                                         | 增加使用真实后端、PostgreSQL 和 Redis 的集成 E2E；模拟测试继续用于快速 UI 回归                                                                                | CI 至少覆盖注册/登录、租户、流程发布、发起、选人、审批和跟踪图刷新                                 |
| P1     | 核心页面和样式文件过大               | 修改成本、样式覆盖和回归风险持续上升                                                               | 已拆出 Agent Schema 字段编辑器、Agent Provider 表格、流程设计器 Agent 配置面板和对应 composable；继续拆分 Agent 运行详情、设计器属性面板、版本面板、派单编辑器、实例任务操作和页面级 composable | Agent 定义、版本、Provider、运行记录形成独立组件/composable；View 保持编排职责；不再新增千行级文件 |
| P1     | 存在跨 feature 直接引用内部 API      | 功能边界逐渐形成网状依赖                                                                           | 为 feature 建立 `index.ts` 公共入口和依赖规则；跨域组合放在页面用例层                                                                                         | ESLint 或架构测试禁止引用其他 feature 的内部路径                                                   |
| P1     | API 类型和错误契约依赖手工同步       | 后端字段变化容易造成运行时错误                                                                     | 根据 OpenAPI 生成 DTO 客户端，保留领域展示模型和转换层                                                                                                        | CI 能检测契约漂移，页面不直接依赖未经转换的后端原始模型                                            |
| P1     | Trace ID 和错误诊断入口仍需完善      | Agent 运行详情已展示安全错误摘要、Trace ID、Attempt、Step 和恢复决策，普通请求错误仍需统一诊断入口 | 统一错误详情组件，增加复制 Trace ID、权限分级详情和流程操作错误关联                                                                                           | 所有后端失败可通过 Trace ID 定位，敏感信息不暴露给用户                                             |
| P1     | 租户或权限变化后不会重新校验当前路由 | 用户切换到低权限租户或权限被撤销后仍停留在原页面，直到接口返回 403                                 | 权限刷新和租户切换成功后重新评估当前路由；无权时跳转到第一个可访问页面，并清理失效页面状态                                                                    | 租户切换、角色撤销和权限撤销用例均不会停留在无权页面，也不会产生连续 403                           |
| P2     | 中文文案直接分散在组件中             | 国际化和术语统一成本上升                                                                           | 在产品范围稳定后引入消息目录，先统一业务术语和状态映射                                                                                                        | 页面文案从统一资源读取；日期、数字和时区可配置                                                     |
| P2     | 缺少自动可访问性检查                 | 键盘、焦点、颜色对比和读屏问题可能回归                                                             | 已在核心 Playwright 流程接入 axe 并补充侧边栏键盘/语义回归；继续扩展到 Agent、流程实例和租户主页面                                                           | 核心页面无严重可访问性违规；流程设计器提供可理解的替代操作                                         |
| P2     | BPMN 和 UI 依赖使主资源偏大          | 弱网络下首屏和设计器打开时间增加                                                                   | 已将 BPMN 样式和字体改为设计器/流程图路由级加载，并建立包体预算；继续评估 Element Plus 样式按需加载和更细粒度拆包                         | 普通列表页面不加载设计器代码；包体超限在 CI 中明确失败                                               |

## 分阶段路线

### 阶段 A：质量和安全闭环

1. 增加前端 CI 和统一 `verify` 命令。
2. 建立真实后端 E2E 环境和最小关键路径。
3. 与后端共同建立 OpenAPI 契约、错误码和 Trace ID。
4. 设计并迁移安全会话存储方案，增加 CSP 和安全头验证。

### 阶段 B：可维护性治理

1. 拆分 `ProcessDesignerView`、`AssignmentRuleListView`、实例详情和超大样式文件。
2. 建立 feature 公共 API、依赖检查和共享组件目录准入规则。
3. 将状态标签、筛选区、表单布局、用户搜索和流程版本展示收敛为统一组件。
4. 加入包体预算、可访问性测试和关键视觉回归测试。

### 阶段 C：工作流与 Agent 体验

1. 在后端完成任务中心、组织、表单、通知和 SLA 后再增加对应前端域。
2. Agent 页面只展示受治理的定义、运行、步骤、检查点、工具授权和人工确认，不暴露原始密钥。
3. Agent 长任务必须使用可恢复的服务端状态和事件刷新，不能依赖页面轮询保存真实执行状态。
4. 看板、Agent 编排和 BPMN 设计器应复用统一身份、租户、权限、审计和设计系统。

## 变更完成标准

后续任何非文档变更至少必须满足：

- 所属 feature、状态归属和跨域依赖明确。
- 租户切换、权限变化、401、网络失败和空数据状态得到处理。
- 新交互具有加载、成功、失败、禁用、防重复提交和键盘操作状态。
- 同类控件复用设计 Token 和共享组件，不新增随意覆盖样式。
- 新业务规则有单元测试；关键用户路径有 Playwright 测试。
- `npm run check` 通过；发布前执行完整 E2E，不得通过关闭 lint 或类型规则规避失败。
- API 变化同步更新契约、转换模型、Query Key 和缓存失效策略。
- 大型功能先更新本治理文档或架构决策，再开始实现。

## 当前验证基线

2026-08-23 的评审验证结果：

- 格式、Oxlint、ESLint、类型检查、生产构建、包体预算、33 个 Vitest 测试和 14 个 Chromium Playwright 场景全部成功。
- 当前 Playwright 使用模拟 API，因此不能替代真实前后端集成测试。
